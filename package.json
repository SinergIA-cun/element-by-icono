{
  "name": "element-by-icono",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "One-page conversion site for ELEMENT by ICONO (Cancun, boutique apartments)",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "test": "vitest run --passWithNoTests"
  },
  "devDependencies": {
    "playwright": "^1.61.1",
    "vite": "^8.1.5",
    "vitest": "^4.1.10"
  }
}
